<!Doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>Show message</title>
</head>
<body>
  <h2>works</h2>
      <% for( let row of data ) { %>
        <tr>
            <td><%= row.id %></td>
            <td><%= row.name %></td>
            <td><%= row.artist_id %></td>
            <td><%= row.cultual_assets %></td>
        </tr>
      <% } %>
  </table>
</body>
</html>
